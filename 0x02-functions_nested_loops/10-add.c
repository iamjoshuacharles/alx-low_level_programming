#include "main.h"

/**
* add - a function that adds two integers and returns the result
* @one: first number input
* @two: second number input
* Return: fa + mi
*/

int add(int fa, int mi)
{
	return (fa + mi);
}
